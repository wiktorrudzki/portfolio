type StylesOptions = {
  section: string;
  sectionAnimation: string;
  divWrapper: string;
  divContainer: string;
  image: string;
  demo: string;
  demoContainer: string;
  demoContainerHeader: string;
  demoContainerP: string;
  p: string;
};

type SectionTopStyles = { dark: StylesOptions } | { light: StylesOptions };
